{% extends 'base.html.twig' %}

{% block title %}Edit Registration{% endblock %}

{% block body %}
    <h1>Edit Registration</h1>
    <div class="container"  style="justify-content: center; text-align: center; background-color: #f8f9fa; text-center">
    {{ include('vu_registration/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_vu_registration_index') }}" class="btn btn-secondary" >back to list</a>

    {{ include('vu_registration/_delete_form.html.twig') }}

    </div>
    </div>
{% endblock %}
