{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    <h1>Registration</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ registration.id }}</td>
            </tr>
            <tr>
                <th>RegistrationDate</th>
                <td>{{ registration.registrationDate ? registration.registrationDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ registration.status }}</td>
            </tr>
        </tbody>
    </table>
    <div class="btn-group">
    <a href="{{ path('app_vu_registration_index') }}"class="btn btn-secondary">back to list</a>

    {# <a href="{{ path('app_vu_registration_edit', {'id': registration.id}) }}" class="btn btn-secondary">edit</a> #}

    {{ include('vu_registration/_delete_form.html.twig') }}
{% endblock %}
